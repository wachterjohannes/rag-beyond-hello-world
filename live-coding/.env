# Default values, committed to git. Do NOT put real secrets here.
# Override anything in this file via .env.local, which is git-ignored.

# Cohere covers the LLM, embeddings and reranker for this demo.
# Get a key at https://dashboard.cohere.com/api-keys
COHERE_API_KEY=
